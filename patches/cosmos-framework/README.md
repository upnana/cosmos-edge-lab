# Patches into local `cosmos-framework`

These files are **lab-owned adapters** for SO-101 stack3cam WAM training.
They are copied into a local engine checkout by:

```bash
bash scripts/sync_patches.sh
```

Do not treat this directory as a full framework fork — only the pieces we need
to answer our experiment question.
