**主因：只训了 10 iter**，模型几乎还没学到这套叠块动力学；生成更像「首帧附近轻微晃动」，不是 GT 里的抓取/叠放。

其余差异来源（按重要性）：

1. **训练量不够**  
   ~108 clips × 10 step，生成通路（`moe_gen` / `vae2llm` / `llm2vae` 等）只做了 smoke，远不足以对齐 teleop 时序。

2. **任务不同**  
   - GT：真机 LeRobot front 录像  
   - Pred：单张首帧 + caption 的 **I2V 采样**（扩散采样，非回放）  
   所以不会、也不该逐帧等于 GT。

3. **时长/规格不对齐**  
   - Pred：~2s，49 帧，24fps，832×480  
   - GT 8s：8s，30fps，640×480  
   Prompt 里写了 `duration: 8s`，但推理用的是 `--num-frames 49`（约 2s），语义长度也对不上。

4. **条件信息弱**  
   只有一张 front 首帧 + 结构化文字，没有关节/动作轨迹；10 iter 后模型还几乎不会把「白→蓝→黑」落成具体运动。

5. **评估样本偏乐观**  
   ep119 来自训练同源数据，即便过拟合也只会「像一点」，不会复现完整操作。

**一句话：** 现在的 gap 主要是「管线 smoke 尚未学会任务」，外加 I2V 采样 vs 真机录像、2s vs 8s 的设定差；不是导出或解码坏了。要缩小差距，需要拉长 Vision SFT，并用 held-out episode、对齐 `num_frames`/时长再比。
